# 线下签名授权流程
```shell
线下签名授权：
    1 (授权)可以线下签名进行，签名信息可以在执行接收转账交易时提交到链上(permint)，让授权和转账在一笔交易内完成，
    2 同时转账交易也可以由接收方（或者其他第三发）来提交，也避免了用户（ERC20拥有者）需要有 ETH 依赖。
    3 流程逻辑
    3.1 利用 metamask 获取签名信息，调用 draft-ERC20Permit 合约的 permit 方法验证签名，之后调用 _approve 方法进行授权
    3.2 再调用 deposit 方法进行转账和存款
```
